<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.LabFinal" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryVariant">@color/color_primary_variant</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/color_Secondary</item>
        <item name="colorSecondaryVariant">@color/color_Secondary_variant</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->

        <!-- Activity window background color -->
        <item name="android:windowBackground">@color/white</item>

        <!-- default text color -->
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="BaseButton" parent="android:style/Widget.Button">
        <item name="android:textSize">15sp</item>
        <item name="android:stateListAnimator">@null</item>
    </style>

    <style name="RectangleButton" parent="BaseButton">
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@drawable/button_rectangle_selector</item>
    </style>

</resources>